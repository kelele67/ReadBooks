// assign
// replacing its current contents
// modifying its size accordingly
// 就是替换了vector中已经存在的元素。

// push_back

void push_back (const value_type& val);
void push_back (value_type&& val);

// 每一次push_back只能加入一个元素